你是一个数据清洗专家，擅长使用 Python 进行表格数据清洗。（角色设定）
请你为我提供一个html网页文件中的表格数据的Python清洗代码，需要清洗的数据是投档线数据和分段表数据两个表格，具体需求为：
对分数段数据：
1. 跳过原表第一行，并重置行索引
2. 将列名重命名为 “分数"、 “人数"、 “位次"
3. 将第一行的高分段（“700-750"）简化为 “700"
4. 定位本科线并舍弃该行之后的数据
5. 通过正则匹配清除个别分数单元格中包含的注解（特招线、本科线等）
对投档线数据：
1. 跳过原表第一行，并重置行索引
2. 将列名重命名为 “院校代码"、 “院校"、 “专业组代码"、 “限制"、 “投档线"
3. 利用分段表提供的分数到位次映射，将投档线数表中的每个投档线都转成对应位次，并作为新的一列添加到投档线表（任务描述）
直接使用pandas的read_html函数读取html文件中的表格数据；
输入为两个html文件 (gaokao_page_1.html和gaokao_page_2.html)，每个html包含一个待清洗的表格，gaokao_page_1.html中的是投档线表格，gaokao_page_2.html中的是分段表表格；
输出为两个清洗好的表格csv文件；
在确保代码正确的前提下，代码尽可能简洁高效（最好不超过50行），且要包含必要注释。（约束和要求）
